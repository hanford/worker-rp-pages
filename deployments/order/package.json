{
  "name": "order",
  "scripts": {
    "check": "tsc --noEmit",
    "dev": "next dev -p $PORT",
    "start": "next start",
    "build": "next build && next export && next-static-manifest && mv ./out/next-static-manifest.json ./functions/ && cp -r functions out",
    "deploy": ""
  },
  "dependencies": {
    "next": "12.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.14.0",
    "@types/node": "^18.6.3",
    "@types/react": "17.0.2",
    "@types/react-dom": "^17.0.2",
    "next-static-manifest": "^0.7.0",
    "typescript": "^4.7.4"
  }
}
